Read the `audit.md` to understand the prioritized phases and tasks. Then check `docs/tracking.md` for the current status and identify the next pending task in Phase 1. Work on that task until it is fully implemented and verified, keeping changes minimal and focused. Once the task is complete, update the tracking document to mark it as `[x]` and commit using conventional commit format (e.g., `feat(security): add CSRF protection to all forms`). Do not combine multiple tasks in one commit. After each commit, re-read the audit to adjust task scoping if needed, and continue with the next pending item.